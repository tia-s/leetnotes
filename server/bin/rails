#!/usr/bin/env ruby
APP_PATH = File.expand_path("../config/application", __dir__)
require_relative "../config/boot"
require "rails/command"
Rails::Command.invoke("server") if ARGV.empty?
Rails::Command.invoke(*ARGV) unless ARGV.empty?