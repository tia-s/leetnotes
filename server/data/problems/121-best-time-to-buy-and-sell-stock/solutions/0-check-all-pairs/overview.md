A brute-force strategy to explore every possible outcome. Since a valid transaction requires buying on one day and selling on a future day, the algorithm iterates through every element as a potential "buy" price and then compares it against every subsequent element as a potential "sell" price. By calculating the difference for every single pair $(i, j)$ where $j > i$, it guarantees finding the maximum possible profit.